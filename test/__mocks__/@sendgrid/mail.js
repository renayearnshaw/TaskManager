module.exports = {
    // Mock out the two functions to do nothing - this ensures that no emails
    // will be sent when accounts are created and deleted during testing
    setApiKey() {

    },
    send() {

    }
}
